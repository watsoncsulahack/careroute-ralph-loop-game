# RALPH Loop (ChatDev-style)

Run iterative planning rounds with Orchestrate:

```bash
export IBM_WATSON_ORCHESTRATE_TOKEN=...
./orchestration/ralph-loop.sh 6
```

Outputs are written to `docs/orchestrate-rounds/ROUND-XX.md`.
